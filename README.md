# zettelschrift
A semantic extension for common markup languages (like Markdown, Org, and Neorg).  Adds typed wikilinks (optionally convertible to notes themselves), and Folgezettel logic, without breaking compatibility with your existing ecosystem. Provides agnostic tools for visualisation and conversion.
