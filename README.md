# zettelschrift
An agnostic, semantic extension for common markup languages (like Markdown, Org, and Asciidoc).  Adds typed wikilinks (optionally convertible to notes themselves), and Folgezettel logic, without breaking compatibility with your existing ecosystem. Provides agnostic tools for visualisation and conversion.
